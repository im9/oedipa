{
  "name": "oedipa-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "M4L host layer for Oedipa. Bridges Max transport and the Tonnetz engine: owns WalkState, resolves step→triad, and shapes MIDI note events. Loaded by [node.script] in the .amxd; unit-tested standalone under node --test.",
  "scripts": {
    "test": "node --test host.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
