{
  "name": "oedipa-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Tonnetz engine for Oedipa (M4L target). Runs in Node.js for tests and (via dist/) in Max for Live jsui for the device.",
  "scripts": {
    "build": "tsc",
    "test": "node --test tonnetz.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
